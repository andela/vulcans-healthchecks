machine:
  python:
    version: 3.6.1
test:
  pre:
    - python manage.py migrate
    - python manage.py collectstatic --no-input
  override:    
    - python manage.py test
  post:
    - coverage run --omit="*/tests/*" --source=hc manage.py test    
    - codecov 
deployment:
  test:
    branch: ft-dashboard-unresolved-stuff-147180987
    heroku:
      appname: cd-test